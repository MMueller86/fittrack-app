// Navigation shell — 4 bottom tabs (Home | Nutrition | Recipes | Profile).
// Weight is a stack screen pushed from the Home tab, not its own tab.
// Auth/Onboarding stacks are wired in M2 — Home tab is shown directly in M1.

export { RootNavigator } from './RootNavigator';
