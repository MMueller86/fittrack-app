# FitTrack – User Profile Wizard PAL Update

This package contains the updated calculation rules using PAL derived from steps.

The important change:

- Steps are mapped to PAL.
- PAL is used as `BMR × PAL`.
- Step calories are not added separately.
- Training is added as a separate training-day bonus.
