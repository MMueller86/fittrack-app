{
  "name": "fittrack-app",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "mobile",
    "backend",
    "shared"
  ],
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "build:backend": "npm run build --workspace=backend",
    "typecheck": "npm run typecheck --workspace=backend --workspace=shared --workspace=mobile"
  }
}
